services:
  - type: web
    name: shvy-site
    env: python
    buildCommand: pip install -r requirements.txt && python manage.py migrate --noinput
    startCommand: gunicorn shvy.wsgi --bind 0.0.0.0:$PORT